<%= bootstrap_form_for @user do |f| %>

    <%= f.text_field :email, icon: "envelope" %>
    <%= f.text_field :name, icon: "user" %>
    <%= f.text_area :bio, icon: "pencil" %>
    <%= f.submit :class => "btn btn-warning" %>

<% end %>