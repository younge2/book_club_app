<div class="container">
	<div class="well">

		<h1>BookClubs</h1>

		<% @clubs.each do |club| %>

			<h3><%= link_to club.name, :controller => "clubs", :action => "show", :id => club.id %></h3>
			<p><%= club.blurb %></p>
			



		<% end %>
	</div>
</div>

